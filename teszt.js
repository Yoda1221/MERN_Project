/*
const ts = []

const compareNumeric = (a, b) => { return Math.abs(a) - Math.abs(b) }

function teszthofok(ts) {
    if (typeof tx !== 'undefined' || ts.length > 0 ) {
        const sortedTs = ts.sort(compareNumeric)
        if(ts.length === 1) {
            console.log(sortedTs[0])
        } else {
            if (Math.abs(sortedTs[0]) === Math.abs(sortedTs[1])) {
                if (sortedTs[0] < 0 && sortedTs[1] < 0 ) {
                    console.log(sortedTs[0])
                } else {
                    console.log(Math.abs(sortedTs[0]))
                }
            } else {
                console.log(sortedTs[0])
            }
        }
    } else {
        console.log(0)
    }
}

teszthofok(ts)

const messages  = ['Ab', 'bcZ']
function teszt2() {
    let newMessage

    messages.map( msg  => {
        console.log(msg)
        console.log(messages.indexOf(msg))
        if (msg.charAt(0) === "A") {

        }
        if (msg.charAt(msg.length - 1) === "z") {

        }
    })

    console.log(newMessage)
}

teszt2() */
